#include "MainToRun.h"

int main() {
    /*
     * The only thing i do here is to call the Class make everything to happen.
     * this is for let the main look good.
     */
    MainToRun my_main_to_run;
    my_main_to_run.run();

    return 0;
}
